
# RoboVis



This is a library for visualization


## Instalation

* Install raylib
    
```bash
git clone 
cd raylib
mkdir build
cd build
cmake ..
make
sudo make install
```

* Install third party libraries

```bash
git submodule update --init --recursive
```